CREATE TABLE Administrador (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    senha VARCHAR(100)
);

CREATE TABLE Hotel (
    id INT AUTO_INCREMENT PRIMARY KEY,
    categoria VARCHAR(50),
    localizacao VARCHAR(100),
    titulo VARCHAR(100),
    descricao TEXT,
    classificacao DECIMAL(3,2),
    imagens TEXT
);

CREATE TABLE Usuario (
    id INT AUTO_INCREMENT PRIMARY KEY,
    cpf VARCHAR(14) UNIQUE,
    nome VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    senha VARCHAR(100)
);

CREATE TABLE CategoriaQuarto (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50),
    descricao TEXT,
    restricoes TEXT,
    preco DECIMAL(10,2)
);

CREATE TABLE Quarto (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_categoria INT,
    disponibilidade BOOLEAN,
    FOREIGN KEY (id_categoria) REFERENCES CategoriaQuarto(id)
);

CREATE TABLE Reserva (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT,
    id_hotel INT,
    id_quarto INT,
    preco DECIMAL(10,2),
    numero_quarto INT,
    FOREIGN KEY (id_usuario) REFERENCES Usuario(id),
    FOREIGN KEY (id_hotel) REFERENCES Hotel(id),
    FOREIGN KEY (id_quarto) REFERENCES Quarto(id)
);

CREATE TABLE ComentarioAvaliacao (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_usuario INT,
    id_hotel INT,
    pontuacao DECIMAL(3,2),
    texto_comentario TEXT,
    data_comentario DATE,
    FOREIGN KEY (id_usuario) REFERENCES Usuario(id),
    FOREIGN KEY (id_hotel) REFERENCES Hotel(id)
);

INSERT INTO Administrador (nome, email, senha)
VALUES ('Nome do Administrador', 'email@exemplo.com', 'senha');

INSERT INTO Hotel (categoria, localizacao, titulo, descricao, classificacao, imagens)
VALUES ('Categoria do Hotel', 'Localização do Hotel', 'Título do Hotel', 'Descrição do Hotel', 4.5, 'URL da imagem');

INSERT INTO Usuario (cpf, nome, email, senha)
VALUES ('12345678901', 'Nome do Usuário', 'usuario@exemplo.com', 'senha');

INSERT INTO CategoriaQuarto (nome, descricao, restricoes, preco)
VALUES ('Categoria do Quarto', 'Descrição da Categoria', 'Restrições do Quarto', 100.00);

INSERT INTO Quarto (id_categoria, disponibilidade)
VALUES (1, TRUE);

INSERT INTO Reserva (id_usuario, id_hotel, id_quarto, preco, numero_quarto)
VALUES (1, 1, 1, 100.00, 101);

INSERT INTO ComentarioAvaliacao (id_usuario, id_hotel, pontuacao, texto_comentario, data_comentario)
VALUES (1, 1, 4.5, 'Excelente hotel, recomendo!', '2024-02-17');
